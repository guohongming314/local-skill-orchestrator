interface:
  display_name: "Local Skill Orchestrator"
  short_description: "Bootstrap verified project AI capabilities"
  default_prompt: "Use $bootstrap-skill to initialize this repository's project-local Codex capabilities safely in the current conversation."
policy:
  allow_implicit_invocation: true
