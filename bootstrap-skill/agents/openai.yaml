interface:
  display_name: "Local Skill Orchestrator"
  short_description: "Bootstrap verified project AI capabilities"
  default_prompt: "Use $bootstrap-skill to inspect this repository, preview its project AI configuration, and apply it safely."
policy:
  allow_implicit_invocation: true
