interface:
  display_name: "Local Skill Orchestrator"
  short_description: "Bootstrap and discover verified project capabilities"
  default_prompt: "Use $bootstrap-skill to initialize this repository's project-local Codex capabilities safely in the current conversation, asking separately before remote discovery and before installation."
policy:
  allow_implicit_invocation: true
